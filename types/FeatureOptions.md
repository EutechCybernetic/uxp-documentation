# FeatureOptions

## Definition

```tsx
export interface FeatureOptions {
    /** Overrides the key derived from the config — use it when several calls stand or fall together. */
    key?: string;
}
```

## Usage

```tsx
import { FeatureOptions } from 'uxp/components';
```

