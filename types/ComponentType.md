# ComponentType

## Definition

```tsx
export enum ComponentType {
    Widget     = 'widget',
    UI         = 'ui',
    Background = 'background',  // NEW
    Component  = 'component',   // NEW — unified type segment for new registerComponent() calls
}
```

## Usage

```tsx
import { ComponentType } from 'uxp/components';
```

