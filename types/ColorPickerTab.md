# undefined

Complex-mode dropdown views, surfaced as the 4-icon switcher:
palette / manual input / theme variables / presets.


## Definition

```tsx
type ColorPickerTab = 'palette' | 'input' | 'theme' | 'presets'
```

## Usage

```tsx
import { undefined } from 'uxp/components';
```

