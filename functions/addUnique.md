# addUnique


Append values that aren't already present, preserving order. Every source
adds to the selection this way.


## Installation

```tsx
import { addUnique } from 'uxp/components';
```

## Signature

```tsx
function addUnique(list: string[], values: string[]): string[]
```

